apiVersion: v1
kind: Service

metadata:
  name: devops-practice-service

spec:
  selector:
    app: devops-practice

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: NodePort
